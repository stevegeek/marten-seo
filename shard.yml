name: marten_seo
version: 0.1.2

authors:
  - Stephen Ierodiaconou <stevegeek@gmail.com>

crystal: '>= 1.20.1'

license: MIT

dependencies:
  marten:
    github: martenframework/marten
    # marten publishes no version tags; match marten-smtp-emailing's version-based
    # requirement so both resolve. Pin an exact commit via shard.override.yml if needed.
    version: ">= 0.2.0"
  web_escape:
    github: stevegeek/web-escape

development_dependencies:
  sqlite3:
    github: crystal-lang/crystal-sqlite3
  blueprint:
    github: stephannv/blueprint
    version: ~> 1.1
