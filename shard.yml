name: marten_seo
version: 0.1.1

authors:
  - Stephen Ierodiaconou <stevegeek@gmail.com>

crystal: '>= 1.20.1'

license: MIT

dependencies:
  marten:
    github: martenframework/marten
    # Track main (marten has no version tags); smtp-emailing pins branch:main,
    # so a bare `*` here can't coexist with it in the same graph.
    branch: main
  web_escape:
    github: stevegeek/web-escape

development_dependencies:
  sqlite3:
    github: crystal-lang/crystal-sqlite3
  blueprint:
    github: stephannv/blueprint
    version: ~> 1.1
